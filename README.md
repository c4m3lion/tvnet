# tvnet

A new Flutter project.

## Usefull

showsnackbar
MyMessage().showSnack();
```aidl
ScaffoldMessenger.of(context)
        ..removeCurrentSnackBar()
        ..showSnackBar(
          SnackBar(content: Text(status)),
```